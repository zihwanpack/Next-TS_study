'use client';
import ModalBackdrop from './_components/modal-backdrop';

const Modalpage = () => {
  return (
    <ModalBackdrop>
      <dialog open></dialog>
    </ModalBackdrop>
  );
};

export default Modalpage;
