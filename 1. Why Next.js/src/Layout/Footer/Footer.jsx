import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <div className={styles.contents}>
        <h2 className={styles.title}>열심히 만들어 봅시다.</h2>
      </div>
    </footer>
  );
};

export default Footer;
